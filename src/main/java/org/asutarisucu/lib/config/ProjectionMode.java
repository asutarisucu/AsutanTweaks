package org.asutarisucu.lib.config;

/** Camera projection used by Clear Block Render. */
public enum ProjectionMode {
    /** Orthographic — no perspective distortion, the classic isometric look. */
    ISOMETRIC,
    /** Standard perspective projection with a configurable FOV. */
    PERSPECTIVE
}
